mid: "{{context.mid}}"
reference: "{{reference}}"
name: "{{name}}"
callback: "{{callback}}"
cardNumber: "cardNumber}}"
cvv: "{{cvv}}"
expiry: "{{expiry}}"
amount: "{{amount}}"
currency: "{{currency}}"
products:
{{#each products}}
  - productId: "{{productId}}"
    count: {{count}}
    spec: "{{spec}}"
{{/each}}
