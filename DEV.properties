# Environment override configuration (sample)
# Loaded second and overrides same keys in config.properties

Env=DEV
Url=http://localhost:8080

# Optional per-env overrides
workDir=target/work-dev
configDir=src/test/resources/testdefine/V1/DEV/

# Keep DB check off for local quick run unless DB is prepared
passDBCheck=Y
appDBUrl=jdbc:mysql://127.0.0.1:3306/qwire_dev
appDBuser=root
appDBpwd=change_me_dev
