name: callback-check-rules-example
desc: example for complex callback JSON validation rules

check:
  callbackCheck:
    rules:
      - path: "$.status"
        eq: "SUCCESS"

      - path: "$.result.items[0].id"
        eq: "@id"

      - path: "$.result.items[*].amount"
        contains: "100"
